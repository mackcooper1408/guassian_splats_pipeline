# Python dependencies for Gaussian Splatting pipeline
numpy>=1.23.0
opencv-python>=4.8.0
pillow>=10.0.0
tqdm>=4.65.0
plyfile>=0.8.1
matplotlib>=3.7.0

# torch + torchvision are installed separately in the Dockerfile
# with the cu121 CUDA index — do not add them here.

# Utilities
pyyaml>=6.0
argparse
pathlib
